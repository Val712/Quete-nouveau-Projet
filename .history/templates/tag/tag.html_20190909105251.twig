{% extends 'base.html.twig' %}

{% block title %}{{ tag.name }}{% endblock %}

{% block stylesheets %}{% endblock %}

{% block body %}

<p> Les articles du Tag :  {{tag.name}}

<div> 
{% for article in articles %}
    <p>{{ article.id }}</p>
    <p>{{ article.title }}</p>
    <p>{{ article.content }}</p>

  
{% endfor %}
</div>

<a href="{{ path('app_index') }}">
    Back to homepage.
</a>

{% endblock %}

